# GeoVision AI v21 — Clean UI

Presentation cleanup based on the supplied screenshots:

- Compact header, sidebar, KPI cards and result status.
- Shorter headings, labels and empty states; repeated introductory text removed.
- Population impact in one metric row, followed by the map and tower priorities.
- Scenario settings, tower details, sources/model information and diagnostics collapsed by default.
- Source status, simulation context and full JSON provenance retained.

Model calculations, datasets, weights, provider integrations and export fields are unchanged. Two existing UI checks were updated to match the new labels.

Extract this project into a new folder and launch `run_windows.bat`. Existing setup requirements still apply; see `README.md`.
